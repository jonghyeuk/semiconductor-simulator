# 검증 게이트 · 물리 수정 내역

빌드가 통과해도 **숫자가 틀린 것**은 잡히지 않는다. 이 문서는 이 레포에 붙인
검증 장치와, 그 과정에서 찾아 **고친** 물리 오류를 정리한 것이다.

## 한 줄로 돌리기

```bash
npm run verify
```

`lint` → `lint:physics` → `verify:physics` → `test` → `build` 순으로 돌고,
하나라도 실패하면 배포하지 않는다. CI(`.github/workflows/deploy.yml`)도 이걸 관문으로 쓴다.

| 스크립트 | 잡는 것 |
|---|---|
| `npm run lint` | 에러 0 유지 (case 블록 변수 누출, `==` 등). 기존 경고 187건은 상한으로 묶어 둠 |
| `npm run lint:physics` | `src/physics`, `scripts` 는 경고 0 |
| `npm run verify:physics` | 회귀 대조 58,874개 + 물리 수정으로 값이 얼마나 바뀌었는지 보고 |
| `npm run test` | 물리 성질 검증 279개 |
| `npm run verify:guides` | 리팩터링이 정적 가이드 HTML을 바꿨는지 (55페이지) |

가이드 대조는 리팩터링할 때만 쓴다:

```bash
npm run verify:guides:snapshot   # 변경 전
npm run build-guides             # 변경 후 재생성
npm run verify:guides            # 차이 보고
```

## 구조

계산식은 컴포넌트에서 `src/physics/` 로 뽑아냈다. 컴포넌트는 state 를 묶어 넘기는
얇은 래퍼만 남긴다.

```
src/physics/
  oxidation.js    doping.js     vacuum.js
  plasma.js       etching.js    lithography.js
  pecvd.js        rta.js        cleaning.js
  __tests__/      # 모듈별 성질 테스트
```

원칙:

- 순수 함수. 랜덤은 인자로 주입받고 기본값을 `Math.random` 으로 둔다
  (화면 동작은 그대로, 테스트는 결정적).
- 스냅샷 테스트 금지. `expect(x).toBe(0.0234)` 대신 "온도가 오르면 두꺼워진다",
  "회전수 4배면 두께 절반" 같은 성질을 검증한다.

`verify:physics` 의 회귀 대조는 **물리 수정 대상이 아니었던** 함수만 본다.
그 함수들은 컴포넌트에서 모듈로 옮기기 이전 코드와 값이 한 자리도 달라지면 안 된다.

## 고친 물리 오류 7건

### ① 열산화: Deal-Grove 선형 영역이 사라져 있었음

`src/physics/oxidation.js` — `calculateOxideGrowth`

`A` 는 `B` 와 `B/A` 의 비인데, 코드는 `A = 165·exp(−2.0/kT)` 처럼 지수를 따로
곱하고 있었다. 그 결과 1000°C 에서 `A ≈ 2e-6 nm` 로 사실상 0이 되어 두께식이
모든 시간에서 `x = √(Bt)` — 순수 포물선이 됐다. 실제 열산화는 초기에 계면 반응이
律速이라 `x ∝ t` 로 시작해 나중에 `x ∝ √t` 로 넘어간다.

**수정**: 문헌 상수(Plummer, <100> Si)로 `B` 와 `B/A` 를 각각 세우고
`A = B/(B/A)` 로 얻는다. 두께가 문헌값의 ±5% 안에 든다.

가스 유량도 함께 고쳤다. 예전에는 완성된 두께에 유량 계수를 그냥 곱해서
`x² + A·x = B·t` 관계가 깨졌다. 유량은 표면 산화종 농도를 통해 `B` 와 `B/A` 에
**함께** 들어가므로, 시간이 스케일되는 것과 같다.

상한도 1000 nm → 5000 nm 으로 올렸다. 예전 상한은 wet 1200°C 180분(≈1.5 μm)을 잘라먹었다.

### ② 이온주입: 질량에 따른 비정 순서가 뒤집혀 있었음

`src/physics/doping.js` — `calculateImplantParams`

환산 에너지 분자에 타겟 질량 M₂ 대신 **이온 질량 M₁** 을 썼고, 거기에 더해
`Rp` 앞에 `(M₁/M₂)` 를 곱했다. 그래서 무거운 이온일수록 깊게 박히는 —
실제와 정반대의 — 결과가 나왔다.

**수정**: LSS/ZBL 적분 모델로 다시 세웠다.

- 환산 에너지 `ε = 32.53·M₂·E / (Z₁Z₂(M₁+M₂)(Z₁^0.23 + Z₂^0.23))`
- ZBL universal 핵 저지능 + Lindhard–Scharff 전자 저지능
- `ρ(ε) = ∫₀^ε dε'/(Sn+Se)` 심프슨 적분 → Schiøtt 근사로 투영 비정
- straggle `ΔRp/Rp = (2/3)·√(M₁M₂)/(M₁+M₂)` — 이온마다 달라진다 (예전엔 0.5 고정)

문헌 대조 (50 keV): B 161 nm (문헌 160), P 67 nm (62), As 38 nm (33).

### ③ 진공: 배기 시간의 단위가 어긋나 있었고, 압력 의존 속도를 무시했음

`src/physics/vacuum.js` — `calculatePumpingTime`, `calculatePumpingTimeFromCurve`

**단위**: 분모에 `/1000` 이 붙어 있었다. 부피를 m³ 로 받는 식인데 UI 는
`{chamberVolume} L` 로 표시한다. 게다가 펌프 속도는 **m³/h** 인데
(`pumpModels` 곡선과 화면 표시 모두 m³/h) L/s 로 쓰는 식이었다.
100 L 챔버 + 모델 2 의 760 → 1 Torr 배기가 실제 2.7초인데 화면엔 6.1분으로 떴다.

**압력 의존성**: 러프 펌핑 구간에서 배기 속도는 압력에 따라 크게 변한다
(모델 2 기준 760 Torr 에서 250 m³/h, 10 Torr 에서 1720 m³/h — 7배 차이).
그런데 코드는 **목표 압력 한 점의 속도**를 760 → 1 Torr 전 구간에 상수로 썼다.
그러면 시간이 2배 이상 어긋난다.

**수정**:

- `convertM3hToLs` 를 명시적으로 두어 단위 경계를 드러냈다 (1 m³/h = 1000 L / 3600 s).
- `calculatePumpingTimeFromCurve` 로 실제 속도 곡선을 적분한다:
  `V·dP/dt = −S(P)·P` → `t = ∫ V/S(P) · d(ln P)`.
- `calculatePumpingTime` 은 속도가 일정한 경우의 해석해로 남겨 뒀다 (L/s 입력).

배기 시간이 대개 1분 미만이 되므로 UI 는 1분 미만을 초로 표시하도록 고쳤고,
펌프 성능 판정 임계값(10분/20분 → 0.15분/0.5분)도 실제 분포(10~1000 L ×
모델 1~4 → 0.1초 ~ 54초)에 맞춰 다시 잡았다. 예전 임계값은 어떤 조합으로도
닿지 않아 정상 펌프가 전부 "매우 느림" 으로 떴다.

### ④ 진공: 분자류 영역인데 점성류 식을 쓰고 있었음

`src/physics/vacuum.js` — `calculateConductance`

`C = 3.27e-2·D⁴/L·1000` 은 `D⁴` 의존성(점성류) 인데 압력 항이 없었다.
이 시뮬레이터는 터보펌프로 1e-6 Torr 까지 내려가는 분자류 영역을 다룬다.

**수정**: `C[L/s] = 12.1·D³/L_eff` (공기 20°C, `L_eff = L + 4D/3` 로 관 입구 효과 반영).
"직경 2배면 conductance 8배" 라는 교육적 결론이 이제 맞는다 (예전엔 16배).

### ⑤ 플라즈마: 반사 전력이 입력 전력을 넘었음

`src/physics/plasma.js` — `calculateReflectedPower`

반사계수 분모를 `Z₀` 로 써서 `|Z − 50| > 50` 인 순간 반사 전력이 입력을 초과했다
(에너지 보존 위반).

**수정**: `Γ = (Z_in − Z₀)/(Z_in + Z₀)`, `P_refl = P_in·|Γ|²`. 복소 임피던스를
그대로 넘겨 계산한다. VSWR 도 함께 추가했다 — 화면 표시식이
`1 + √Γ/(1−√Γ)` 로 괄호가 빠져 있었다 (`(1+√Γ)/(1−√Γ)` 여야 한다).

### ⑥ 플라즈마: 자동 매칭이 50 Ω 이하 부하에서 동작하지 않았음

`calculateOptimalLC` 는 `Z_load < 50` 일 때 회로를 뒤집어 계산하는데,
`calculateInputImpedance` 는 승압 토폴로지 하나만 모델링했다. 두 함수가 서로
다른 회로를 가정한 것이다. 슬라이더 기본값이 정확히 50 Ω 이라, 화면에 들어와
"자동 매칭" 만 눌러도 `L = C = 0` 이 설정되고 입력 임피던스가 NaN 으로 떴다.

**수정**: 두 토폴로지를 모두 모델링한다.

- 승압 (`R_L > 50`): `Z_in = jX_L + (R_L ∥ −jX_C)`
- 강압 (`R_L < 50`): `Z_in = (−jX_C) ∥ (R_L + jX_L)`
- `R_L = 50`: 정합망 불필요 (`C = 0` 은 개방으로 취급)

슬라이더 전 범위(10~200 Ω)에서 자동 매칭 후 오차가 0.5% 안이다.

### ⑦ 리소: 스핀 코팅 두께가 회전수를 안 따라갔음

`src/physics/lithography.js` — `calculateSpinCoatResults`

구간별 1차식이었고 2000~4000 rpm 구간은 난수만 돌려 회전수와 무관했다.
2000 rpm 과 4000 rpm 의 기대 두께가 같았다.

**수정**: `t ∝ ω^(−1/2)` (Emslie–Bonner–Peck). 3000 rpm = 1000 nm 로 정규화하고
±1% 런투런 산포를 남긴다. 회전수 4배면 두께가 정확히 절반이 된다.

## 화면 숫자가 얼마나 바뀌었나

`npm run verify:physics` 가 매번 출력한다.

| 항목 | 조건 | 수정 전 | 수정 후 |
|---|---|---|---|
| ① 산화 두께 | dry 1000°C 60분 | 20.1 nm | **54.2 nm** |
| ① 산화 두께 | wet 1000°C 60분 | 250.8 nm | **449.4 nm** |
| ② 이온주입 Rp | B 50 keV | 65.9 nm | **161.4 nm** |
| ② 이온주입 Rp | As 50 keV | 183.8 nm | **37.9 nm** |
| ③ 배기 시간 | V=100L 모델2 760→1 Torr | 6.1분 | **2.7초** |
| ④ Conductance | D=10cm L=100cm | 3,270 L/s | **106.8 L/s** |
| ⑤ 반사 전력 | Z=150Ω, 1000W 입력 | 4,000 W | **250 W** |
| ⑥ 자동 매칭 | 부하 50 Ω | NaN | **50.0 Ω** |
| ⑦ PR 두께 | 2000 rpm | 1000 nm | **1224.7 nm** |
| ⑦ PR 두께 | 5000 rpm | 950 nm | **774.6 nm** |

## 입력 가드

물리적으로 불가능한 출력(NaN·음수·발산)이 나오던 지점들도 함께 막았다.
슬라이더로 대부분 도달하지 않지만, 가드가 없으면 조건이 조금만 바뀌어도 새어 나온다.

| 위치 | 조건 | 수정 전 | 수정 후 |
|---|---|---|---|
| `calculateOxideGrowth` | 음수 시간 / 절대영도 이하 | NaN | 0 |
| `calculateInitialOxideRate` | 음수 두께 | −100 에서 발산 | 0 으로 clamp |
| `calculateDiffusionProfile` | 시간 0 | NaN (0/0) | 확산 없음 |
| `calculateImplantParams` | 에너지 0 / 틸트 90° 이상 | NaN / 1e15 μm | 0 / 89° 로 제한 |
| `calculatePumpingTime` | 음수 부피 | 음수 시간 | 0 |
| `calculateConductance` | 길이 0 / 음수 치수 | Infinity | 유한값 / 0 |
| `calculateInputImpedance` | C = 0 | NaN | 개방으로 취급 |
| `calculateReflectedPower` | 임의의 Z | 입력 전력 초과 | ≤ 입력 전력 |
| `plasmaFactor` | 음수 에너지 | 무한대 이온화도 | 0 |
| `calculateEtchRate` | 파워/가스 0, 음압 | 5 nm/min 바닥 | 0 |
| `calculateSpinCoatResults` | 24000 rpm 초과 | 음수 두께 | 0 으로 점근 |
| `calculateRefractiveIndex` | 비율 과대 | 음수 굴절률 | 1.4 로 clamp |
| `calculateSiNxRefractiveIndex` | 비율 43 초과 | 굴절률 < 1 | 1.8 로 clamp |
| `calculateNSiRatio` | 음수 비율 | 음수 원자비 | 0 으로 clamp |
| `calculateOxideRemovalEfficiency` | 범위 밖 입력 | 음수 효율 | 0 으로 clamp |
| `stepZoneTemperatures` | dt ≥ 1.0초 | explicit Euler 발산 | 지수 해석해 (무조건 안정) |
| `calculateTempProfile` | 냉각 시작 순간 | 냉각 속도 발산 | 뉴턴 냉각 (유한 기울기) |

유한한 입력 전 범위를 훑어 NaN·무한대·물리적으로 불가능한 값이 남아 있지 않음을
확인했다. `NaN`/`Infinity` 를 직접 넣는 경우는 어느 슬라이더로도 도달할 수 없어 막지 않았다.

## 단위 대조

계산 오류만큼 흔한 게 단위 불일치다. 모듈 입출력 단위를 UI 표시와 하나씩 맞춰 뒀다.

| 함수 | 입력 | 출력 | UI 표시 |
|---|---|---|---|
| `calculateOxideGrowth` | °C, 분, sccm | nm | °C(800~1200), 분(10~180), sccm, nm |
| `calculateImplantParams` | keV, deg | **μm** | keV(10~400), nm (컴포넌트가 ×1000) |
| `calculateDiffusionCoefficient` | °C | cm²/s | — |
| `calculatePumpingTime` | L, Torr, **L/s** | 분 | — (내부용) |
| `calculatePumpingTimeFromCurve` | L, Torr, **m³/h 곡선** | 분 | L, m³/h, 초/분 |
| `calculateConductance` | cm, cm | L/s | cm(2~20, 50~300), L/s |
| `calculateEffectivePumpingSpeed` | L/s, L/s | L/s | L/s |
| `calculateInputImpedance` | MHz, nH, pF, Ω | Ω | MHz(0.4~100), nH/pF(10~1000), Ω(10~200) |
| `calculateReflectedPower` | Ω(복소), W | W | W |
| `calculateEtchRate` | sccm, W, mTorr | nm/min | 동일 |
| `calculateSpinCoatResults` | rpm, 초 | nm | rpm(1000~6000), nm |
| `calculateTempProfile` | 초 | °C | 초, °C |

특히 진공 모듈은 **m³/h 와 L/s 가 섞여 있다**. 펌프 모델 곡선과 화면 표시는 m³/h,
배기 시간 식과 conductance 는 L/s 다. 경계에서 반드시 `convertM3hToLs` 를 거쳐야 한다.

## 정확도와 한계 — 교육용 모델이다

**이 시뮬레이터의 숫자는 실제 장비의 계측값과 다르다.** 공정의 경향과 지배 관계를
보여 주는 것이 목적이고, 실제 장비는 챔버 형상·가스 순도·웨이퍼 이력·계측 방식에
따라 아래 오차보다 훨씬 크게 벌어질 수 있다. 공정 조건 산출이나 장비 판단에
그대로 쓰면 안 된다.

아래는 UI 슬라이더 전 범위에서 **실제로 측정한** 문헌 대비 편차다.

### 수치 편차

| 모듈 | 대조 기준 | UI 범위 편차 | 비고 |
|---|---|---|---|
| ① 산화 두께 | Deal-Grove 문헌식 | **0.00%** | 문헌식을 그대로 구현 (아래 모델 한계 참조) |
| ② 이온주입 Rp | 문헌 투영 비정표 | **−25% ~ +28%** | 저에너지 과대, 고에너지 과소 — 아래 표 |
| ④ Conductance | 분자류 `12.1·D³/L` | **−1% ~ −35%** | 입구 보정 때문. 짧고 굵은 관일수록 차이가 큼 |
| ⑥ RF 자동 매칭 | 목표 50 Ω | **0.0% ~ 4.7%** | L·C 를 정수 nH/pF 로 반올림. 고주파일수록 큼 |
| ③ 배기 시간 | 적분 수렴 | **< 0.001%** | 512 분할 기준 |
| ⑦ PR 두께 | `t ∝ ω^(−1/2)` | **0.00%** | 법칙을 그대로 구현 |

**② 이온주입 상세** — LSS/Schiøtt 근사는 저에너지에서 깊이를 과대평가하고
고에너지에서 과소평가한다. 이온이 무거울수록 저에너지 오차가, 가벼울수록
고에너지 오차가 커진다.

| keV | B | P | As |
|---|---|---|---|
| 15 | +7% | +21% | +28% |
| 30 | +7% | +12% | +20% |
| 50 | +1% | +7% | +15% |
| 100 | −8% | −7% | +7% |
| 180 | −19% | −12% | −1% |
| 300 | −25% | −15% | −8% |

**질량 순서(B > P > As > In > Sb)와 에너지 의존성은 전 범위에서 옳다.**
경향 학습에는 문제가 없고, 절대 깊이가 필요하면 SRIM 등 전용 도구를 써야 한다.

**⑥ RF 매칭 상세** — 13.56 MHz(실제 공정 주파수)에서는 최대 0.7% 로 충분하다.
100 MHz 에서 4.7% 까지 벌어지는 건 필요한 L·C 가 작아져 정수 반올림 영향이
커지기 때문이다. 계산식이 아니라 표시 자릿수의 문제다.

### 모델 자체의 한계 (수치 편차와 별개)

식이 맞아도 모델이 담지 않은 물리가 있다. 실제 장비와 벌어지는 주된 이유다.

**산화** — 초기 산화막 오프셋 τ 를 넣지 않았다. 그래서 **dry 산화 30 nm 이하
박막에서는 실측보다 얇게** 나온다 (실제로는 초기 급속 성장 구간이 있다).
Deal-Grove 자체가 20 nm 이하에서는 잘 맞지 않는 것으로 알려져 있다.
결정 방위·도핑·압력 인자는 성장 계산에 들어가지 않고 "영향 인자" 탭의
상대 배율로만 보여 준다.

**이온주입** — 가우시안 분포로만 그린다. 실제 프로파일은 비대칭이라
Pearson IV 분포를 쓴다. **채널링(결정면 정렬로 훨씬 깊이 들어가는 현상)을
모델링하지 않았다.** 틸트 7° 가 기본값인 이유가 채널링 회피인데, 틸트를 0 으로
줘도 깊이가 튀지 않는다. 비정질화·도즈 의존 효과도 없다.

**배기** — 아웃가싱과 가스 부하가 없는 이상적 배기다. 실제로는 1e-3 Torr
아래에서 아웃가싱이 지배해 훨씬 오래 걸리고, 결국 도달 압력이 한계에 걸린다.
등온이라고 가정했고, 챔버-펌프 사이 배관 conductance 제약도 반영하지 않았다.

**Conductance** — 분자류만 계산한다. 이 시뮬레이터는 대기압에서 1e-6 Torr 까지
훑는데, 그중 점성류·전이류 구간은 식이 다르다. 엘보 0.7, 스파이럴 0.4 계수는
실측이 아니라 교육용으로 잡은 값이다.

**플라즈마** — Paschen 곡선은 가스별 실측 테이블 보간이다(Townsend 이론식이 아니다).
이온화도 식은 현상론적 가우시안이라 물리 모델이 아니다. RF 매칭은 부하를
**순수 저항**으로 본다. 실제 플라즈마 부하는 복소 임피던스이고 전력에 따라 변한다.

**스핀 코팅** — 회전수만 반영한다. 실제 두께는 PR 점도·고형분·용매 증발 속도·
스핀 시간에 함께 좌우된다. 3000 rpm = 1000 nm 정규화도 특정 PR 하나를 가정한 것이다.

**식각 · PECVD · 세정 · RTA** — 1차 원리 계산이 아니라 문헌 데이터에 맞춘
경험식이다. 경향(무엇을 올리면 무엇이 오르는지)은 맞지만 절대값은 장비마다 다르다.

## 남겨 둔 모델링 가정

물리 오류는 아니지만 임의로 잡은 값들이다. 바꾸려면 판단이 필요하다.

- **선택비 하한 1.0** (`calculateSelectivity`) — 실제로는 하부층이 더 빨리 식각되는
  선택비 < 1 도 가능하다. 지금은 표시 편의상 1 에서 막는다.
- **균일도 상한 99.5%** (`calculateSpinCoatResults`) — 기본값이 99 이고 모든 분기가
  감점만 하므로 도달할 수 없는 죽은 상한이다.
- **해상도 88~92%** (`calculateSpinCoatResults`) — 공정 파라미터와 무관한 난수다.
- **Drive-in 사전 확산 조건** (`calculateDiffusionProfile`) — 1000°C 30분으로 고정.
- **Paschen 곡선** (`plasma.js`) — 가스별 실측 테이블 보간. 식이 아니라 표다.
- **성장 속도 표시** (`Oxidation.js`) — `두께 / 시간` 이라 구간 **평균** 속도다.
  Deal-Grove 순간 속도 `dx/dt = B/(2x + A)` 와는 다르다.
- **이온주입 고에너지 정확도** — Schiøtt 근사라 가벼운 이온의 고에너지 쪽에서
  오차가 커진다 (B 180 keV 에서 문헌 대비 0.81배). 전 범위 문헌 대비 0.81~1.20배.
- **PECVD 막질 상관식** (`pecvd.js`) — 문헌 데이터에 맞춘 구간별 근사다.

## 남은 정리 대상

`npm run lint` 는 현재 에러 0, 경고 187건이다. 경고는 게이트를 통과시키되 상한
(`--max-warnings 200`) 으로 묶어 늘어나지 않게 했다.

| 규칙 | 건수 | 성격 |
|---|---|---|
| `no-unused-vars` | 154 | 안 쓰는 state/함수. 지우는 건 안전하지만 파일이 커서 별건 |
| `react-hooks/exhaustive-deps` | 33 | Three.js 애니메이션 루프가 많아 의존성 추가가 동작을 바꿀 수 있음 |

두 종류 다 계산값에는 영향이 없다.
